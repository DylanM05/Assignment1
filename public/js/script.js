// Wait for the DOM to be fully loaded
document.addEventListener('DOMContentLoaded', function() {
    // Select the element with the custom class and add the 'loaded' class
    var customElement = document.querySelector('#about-me');
    customElement.classList.add('loaded');
  });
  